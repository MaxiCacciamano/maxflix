import { makeStyles } from '@material-ui/core';
import React from 'react'

export const PayPal = () => {
  return (
    <div >PayPal</div>
  )
}
const useStyle = makeStyles((theme)=>({
  root:{
  }
}))